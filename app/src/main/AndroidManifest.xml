<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.japan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Design.Light">
        <activity android:name=".ActivityHandleHandBook"></activity>
        <activity android:name=".ContentSearch" />
        <activity android:name=".ContentHandbook" />
        <activity android:name=".CardView" />
        <activity android:name=".FinishCourse" />
        <activity android:name=".Lesson" />
        <activity
            android:name=".SplashScreenStartCourse"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".ContentGrammar" />
        <activity android:name=".Course" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>